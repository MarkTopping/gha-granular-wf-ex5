name: Deploy To UAT

run-name: "#${{ github.run_number }}. Deploy to UAT ran on ${{ github.ref_type }} '${{ github.ref_name }}'. Triggered by: ${{ github.triggering_actor }}"

on:
  # Workflow can be manually triggered
  workflow_dispatch:

  workflow_call:
    
jobs:

  derive-tag:
    uses: ./.github/workflows/derive-tag.yml
          
  deploy-to-uat:
    name: Deploy to UAT
    runs-on: ubuntu-latest
    needs: [derive-tag]
    environment: uat
    steps:

      - name: Grab Image Tag
        env:
          docker-tag: ${{ needs.derive-tag.outputs.docker-tag }}
        run: |
          echo "determining the image tag based off whatever versioning logic used"
          echo "tag equals: ${{ env.docker-tag }}"
          
      - name: Deploy to UAT
        run: echo "deploying via helm"
